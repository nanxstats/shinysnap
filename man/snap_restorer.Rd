% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restorers.R
\name{snap_restorer}
\alias{snap_restorer}
\alias{snap_restorers}
\title{Register how an input is restored}
\usage{
snap_restorer(x, fn, session = NULL)

snap_restorers(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{x}{An input id or a binding name, as reported in the snapshot's
\code{bindings} section.}

\item{fn}{The restorer function, or \code{NULL} to remove a registration.}

\item{session}{A Shiny session to register the restorer for that session
only, or \code{NULL} (the default) to register it globally.}
}
\value{
\code{snap_restorer()} returns \code{fn} invisibly. \code{snap_restorers()}
returns a data frame with the columns \code{name} and \code{scope} (\code{"builtin"},
\code{"global"}, or \code{"session"}).
}
\description{
A \emph{restorer} turns the value stored in a snapshot into the message that
the input's client-side binding understands, that is, what the matching
\code{update*Input()} function would send. shinysnap ships restorers for the
inputs of shiny, bslib, and shinyMatrix (see \code{\link[=snap_restorers]{snap_restorers()}}); the
default for everything else is \code{list(value = value)}. Register your own
for an input id or for a binding name (for example \code{"shinyWidgets.pickerInput"}).
}
\details{
\code{fn} is called as \code{fn(id, value, binding, session)} and must return the
message as a list, or \code{NULL} to skip the input (reported as \code{skipped}).
It must not call \verb{update*()} functions itself: those go through
\code{session$sendInputMessage()}, which silently drops messages for inputs
that are not on the page yet. To find the right payload, read the
\verb{update*()} function's source and keep the part that carries the value.

Optionally, the returned list may carry an attribute \code{expect} holding the
value the binding's \code{getValue()} is expected to return after the message
was applied; the client uses it to report \code{mismatched} when the widget
shows something else. By default the message's \code{value} is used.

Resolution order when restoring an input: a session restorer for the id,
a session restorer for its binding, a global restorer for the id or the
binding, the built-in restorer for the binding, the default.
}
\examples{
# A restorer for a hypothetical widget whose update function sends
# list(selected = value):
snap_restorer("mypkg.myInput", function(id, value, binding, session) {
  list(selected = value)
})
snap_restorers()
snap_restorer("mypkg.myInput", NULL)
}
