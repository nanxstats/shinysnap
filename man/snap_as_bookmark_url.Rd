% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interop.R
\name{snap_as_bookmark_url}
\alias{snap_as_bookmark_url}
\alias{snap_as_test_inputs}
\alias{snap_attachment}
\title{Interoperate with bookmarks, tests, and bundles}
\usage{
snap_as_bookmark_url(x, session = NULL, base_url = NULL)

snap_as_test_inputs(x)

snap_attachment(x, id)
}
\arguments{
\item{x}{A snapshot object, a list of snapshot fields, a file path, or
JSON text.}

\item{session}{A Shiny session whose \code{clientData} provides the base URL
(protocol, host, port, and path), or \code{NULL}.}

\item{base_url}{The base URL to prepend, for example
\code{"https://example.org/app/"}. When neither \code{session} nor \code{base_url} is
given, only the query string (starting with \verb{?}) is returned.}

\item{id}{An input id.}
}
\value{
\code{snap_as_bookmark_url()} returns a string. \code{snap_as_test_inputs()}
returns a named list. \code{snap_attachment()} returns a character vector of
file paths (one per uploaded file), or \code{NULL} when the snapshot has no
attachment for \code{id}.
}
\description{
\itemize{
\item \code{snap_as_bookmark_url()} encodes a snapshot the way shiny's URL
bookmarking (\code{enableBookmarking("url")}) does, so that the state can be
opened by reloading the app at that URL. The app must have URL
bookmarking enabled and a UI \emph{function} for the URL to restore; the
query string carries the same \verb{_inputs_} and \verb{_values_} keys that
\code{session$doBookmark()} writes, with input ids in the order stored in
the snapshot.
\item \code{snap_as_test_inputs()} returns the input values as a named list for
\code{shiny::testServer()}: \code{session$setInputs(!!!snap_as_test_inputs(x))}.
\item \code{snap_attachment()} returns the local path(s) of the file(s) a
\code{fileInput()} held when the snapshot was taken, if the snapshot came
from a bundle (see \code{\link[=snap_write]{snap_write()}}) or from the same session.
}
}
\examples{
snap <- snap_unserialize('{
  "format": 1,
  "inputs": {"n": 100, "method": "b", "weights": [0.5, 0.75]},
  "values": {"note": "baseline"}
}')
snap_as_bookmark_url(snap, base_url = "https://example.org/app/")
str(snap_as_test_inputs(snap))
snap_attachment(snap, "upload")
}
