% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui.R
\name{snap_download_button}
\alias{snap_download_button}
\alias{snap_download_handler}
\title{Save-state button and its download handler}
\usage{
snap_download_button(
  id,
  label = "Save state",
  icon = shiny::icon("download"),
  class = NULL,
  ...
)

snap_download_handler(
  id,
  filename = "state",
  format = c("json", "zip"),
  ...,
  snapshot = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{The output id of the button. \code{snap_download_handler()} accepts
a vector of ids, so that several buttons (for example one per tab)
share one definition.}

\item{label, icon, class, ...}{Passed on to \code{shiny::downloadButton()}. In
\code{snap_download_handler()}, \code{...} is passed on to \code{\link[=snap_take]{snap_take()}}.}

\item{filename}{The file name without extension: a string, a function, or
a reactive (for example a text input where the user names the file). It
is reduced to the characters \verb{A-Z a-z 0-9 . _ -}, falls back to
\code{"state"} when empty, and gets the format's extension appended.}

\item{format}{The file format: \code{"json"} (the default) or \code{"zip"} for a
bundle that includes uploaded files (see \code{\link[=snap_write]{snap_write()}}).}

\item{snapshot}{\code{NULL} to take a snapshot when the button is clicked (the
default), a snapshot object, or a function or reactive returning one.}

\item{session}{The Shiny session. Defaults to the current session.}
}
\value{
\code{snap_download_button()} returns a tag. \code{snap_download_handler()}
returns the ids invisibly.
}
\description{
\code{snap_download_button()} is a \code{downloadButton()} with the client script
attached. \code{snap_download_handler()} defines the matching download: it
takes a snapshot with \code{\link[=snap_take]{snap_take()}} when the button is clicked and writes
it with \code{\link[=snap_write]{snap_write()}}. Together they replace the usual
\code{downloadHandler()} boilerplate.
}
\examples{
if (interactive()) {
  library(shiny)

  ui <- fluidPage(
    sliderInput("n", "n", 1, 100, 50),
    textInput("filename", "File name", "state"),
    snap_download_button("save")
  )

  server <- function(input, output, session) {
    snap_enable(app = "demo", version = "1.0.0", exclude = "^filename$")
    snap_download_handler("save", filename = reactive(input$filename))
  }

  shinyApp(ui, server)
}
}
