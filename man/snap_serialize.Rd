% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/io.R
\name{snap_serialize}
\alias{snap_serialize}
\alias{snap_unserialize}
\title{Convert a snapshot to and from JSON text}
\usage{
snap_serialize(
  x,
  format = "json",
  pretty = TRUE,
  unsupported = c("error", "rds"),
  verbose = FALSE
)

snap_unserialize(
  text,
  format = "json",
  trust = FALSE,
  unknown_types = c("error", "keep")
)
}
\arguments{
\item{x}{A snapshot object, as returned by \code{\link[=snap_take]{snap_take()}} or
\code{\link[=snap_unserialize]{snap_unserialize()}}, or a list with (some of) a snapshot's fields,
for example \code{list(inputs = list(n = 5))}.}

\item{format}{The text format. Only \code{"json"} is available.}

\item{pretty}{Pretty-print with two-space indentation (the default) or
emit compact JSON on one line.}

\item{unsupported}{What to do with values the JSON format cannot describe:
\code{"error"} (the default) or \code{"rds"} to embed them as serialized R objects.}

\item{verbose}{Print a message about what was converted or dropped.}

\item{text}{JSON text: a single string or a character vector of lines.}

\item{trust}{Decode embedded serialized R objects (\code{"$type": "rds"})? Only
set this to \code{TRUE} for files from a source you trust.}

\item{unknown_types}{What to do with a \code{"$type"} the reader does not know:
\code{"error"} (the default) or \code{"keep"} to keep the raw parsed value.}
}
\value{
\code{snap_serialize()} returns a single string. \code{snap_unserialize()}
returns a snapshot object of class \code{shinysnap}.
}
\description{
\code{snap_serialize()} writes a snapshot as JSON text in the canonical
shinysnap format; \code{snap_unserialize()} reads it back. These are the
in-memory counterparts of \code{snap_write()} and \code{snap_read()}.
}
\details{
The JSON format is designed to be read and edited by people: doubles are
written with the fewest digits that read back to the same value, integers
as plain digit runs, and everything JSON cannot express directly (the
type of an empty vector, \code{NA}, \code{Inf}, names, dates, matrices, factors,
data frames) as a small object with a \code{"$type"} key.

Values that the format cannot describe (environments, functions, S4 and
R6 objects, unknown classes) are an error by default. With
\code{unsupported = "rds"} they are embedded as base64 serialized R objects
instead; because unserializing arbitrary data is unsafe, reading them back
requires \code{trust = TRUE}, otherwise they decode to \code{NULL} with a warning.
}
\examples{
text <- '{
  "format": 1,
  "app": {"name": "myapp", "version": "2.4.1"},
  "inputs": {
    "dates": {"$type": "Date", "value": ["2024-01-01", "2024-03-01"]},
    "method": "b",
    "n": 100,
    "rate": 0.025,
    "weights": [0.5, 0.75]
  }
}'
snap <- snap_unserialize(text)
snap
str(snap_inputs(snap))
cat(snap_serialize(snap))
}
