% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{snap_diff}
\alias{snap_diff}
\title{Compare two snapshots}
\usage{
snap_diff(a, b)
}
\arguments{
\item{a, b}{Snapshot objects, lists of snapshot fields, file paths, or
JSON text.}
}
\value{
A data frame of class \code{shinysnap_diff} with one row per
difference and the columns \code{id}, \code{section} (\code{"inputs"}, \code{"values"}, or
\code{"meta"}), \code{status} (\code{"added"}, \code{"removed"}, or \code{"changed"}, seen from
\code{a} to \code{b}), and the list columns \code{old} and \code{new} holding the values
(\code{NULL} for the side where the entry is absent).
}
\description{
Lists the inputs, values, and metadata entries that differ between two
snapshots. Values are flattened one level, so a tracked \code{reactiveValues}
stored as \code{prefs} with a changed \code{digits} field shows up as
\code{prefs$digits}.
}
\examples{
a <- snap_unserialize('{"format": 1, "inputs": {"n": 1, "x": "old"},
  "values": {"prefs": {"digits": 3}}}')
b <- snap_unserialize('{"format": 1, "inputs": {"n": 1, "y": true},
  "values": {"prefs": {"digits": 4}}}')
snap_diff(a, b)
}
