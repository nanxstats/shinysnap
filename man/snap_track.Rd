% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/track.R
\name{snap_track}
\alias{snap_track}
\title{Track server-side values}
\usage{
snap_track(
  values,
  fields = NULL,
  name = NULL,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{values}{A \code{reactiveValues} object.}

\item{fields}{Names of the fields to capture, or \code{NULL} for all fields.}

\item{name}{The name to store the values under. Defaults to the name of
the variable passed as \code{values}.}

\item{session}{The Shiny session. Defaults to the current session.}
}
\value{
The (namespaced) name the values are stored under, invisibly.
}
\description{
Registers a \code{reactiveValues} object so that \code{\link[=snap_take]{snap_take()}} captures its
fields (all of them, or the ones named in \code{fields}) under \code{name} in the
snapshot's \code{values} section, and so that a restore can write them back.
}
\details{
Inside a module, \code{name} is namespaced automatically.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    prefs <- reactiveValues(digits = 3L, scientific = FALSE)
    snap_track(prefs)
  }
}
}
