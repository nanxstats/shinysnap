% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ui.R
\name{snap_file_input}
\alias{snap_file_input}
\alias{snap_file_restore}
\title{Restore-state upload and its handler}
\usage{
snap_file_input(id, label = "Restore state", accept = ".json", ...)

snap_file_restore(
  id,
  ...,
  validate = NULL,
  migrate = NULL,
  on_error = c("notify", "modal", "stop"),
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{id}{The input id. \code{snap_file_restore()} accepts a vector of ids, so
that several upload controls share one definition.}

\item{label, accept, ...}{Passed on to \code{shiny::fileInput()}. In
\code{snap_file_restore()}, \code{...} is passed on to \code{\link[=snap_restore]{snap_restore()}}.}

\item{validate}{A function of the snapshot that should \code{stop()} with a
user-facing message when the file is not acceptable. Runs first.}

\item{migrate}{A function \verb{function(snapshot, from_version)} returning a
modified snapshot, for example filling in defaults for values introduced
after \code{from_version} (the app version recorded in the file, or \code{NULL}).
Runs after \code{validate}, before anything is touched.}

\item{on_error}{How to report a file that cannot be restored: a
\code{showNotification()} (the default), a \code{showModal()} dialog, or \code{stop()}.}

\item{session}{The Shiny session. Defaults to the current session.}
}
\value{
\code{snap_file_input()} returns a tag. \code{snap_file_restore()} returns
the ids invisibly.
}
\description{
\code{snap_file_input()} is a \code{fileInput()} with the client script attached.
\code{snap_file_restore()} observes it: when a file is uploaded it reads the
snapshot with \code{\link[=snap_read]{snap_read()}}, runs \code{validate} and \code{migrate}, and calls
\code{\link[=snap_restore]{snap_restore()}}, reporting problems to the user.
}
\examples{
if (interactive()) {
  library(shiny)

  ui <- fluidPage(
    sliderInput("n", "n", 1, 100, 50),
    snap_download_button("save"),
    snap_file_input("restore")
  )

  server <- function(input, output, session) {
    snap_enable(app = "demo", version = "1.0.0")
    snap_download_handler("save")
    snap_file_restore("restore", validate = function(snap) {
      if (is.null(snap$inputs$n)) stop("This file does not contain `n`.")
    })
  }

  shinyApp(ui, server)
}
}
