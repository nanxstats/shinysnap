% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore.R
\name{snap_restore}
\alias{snap_restore}
\title{Restore a snapshot into the running app}
\usage{
snap_restore(
  x,
  session = shiny::getDefaultReactiveDomain(),
  ...,
  inputs = TRUE,
  values = TRUE,
  include = NULL,
  exclude = NULL,
  validate = NULL,
  migrate = NULL,
  check_app = TRUE,
  unknown = c("warn", "skip", "error"),
  timeout = 10,
  settle = 0.3,
  use_restore_context = NULL,
  on_done = NULL
)
}
\arguments{
\item{x}{A snapshot object, a file path, or JSON text.}

\item{session}{The Shiny session. Defaults to the current session.}

\item{...}{Not used; arguments after \code{session} must be named.}

\item{inputs, values}{Restore the inputs / the tracked values? Set one to
\code{FALSE} to restore only the other.}

\item{include, exclude}{Regular expressions matched against fully
namespaced ids, in addition to those configured with \code{\link[=snap_enable]{snap_enable()}}.}

\item{validate}{A function of the snapshot that should \code{stop()} with a
user-facing message when the file is not acceptable. Runs first.}

\item{migrate}{A function \verb{function(snapshot, from_version)} returning a
modified snapshot, for example filling in defaults for values introduced
after \code{from_version} (the app version recorded in the file, or \code{NULL}).
Runs after \code{validate}, before anything is touched.}

\item{check_app}{Refuse files whose app name differs from the one
configured with \code{\link[=snap_enable]{snap_enable()}} (only when both are known).}

\item{unknown}{What to do when inputs never appeared or failed: \code{"warn"}
(one consolidated warning, the default), \code{"skip"} (nothing), or
\code{"error"} (reject the promise).}

\item{timeout}{Seconds after which the browser stops waiting for inputs
that have not appeared.}

\item{settle}{Seconds of quiet (no busy state, no new UI) after which the
browser considers the restore complete.}

\item{use_restore_context}{Prime shiny's \code{restoreInput()} mechanism during
the restore. Defaults to the value configured with \code{\link[=snap_enable]{snap_enable()}}.}

\item{on_done}{A function of the report, called when the restore settles;
a convenience for code that does not want to work with promises.}
}
\value{
A handle of class \code{shinysnap_restore}, invisibly: a list with the
transaction id in \code{txn} and a \code{\link[promises:promise]{promises::promise()}} that resolves to the
restore report in \code{promise}. The handle is deliberately not a promise
itself: shiny waits for a promise returned from an observer before it
flushes, and the report can only arrive after the browser has finished
applying the restore, so returning the promise from an observer would
stall the restore. Use \code{on_done}, \code{\link[=snap_on_restored]{snap_on_restored()}}, or
\code{promises::then(handle$promise, ...)} to work with the report.
}
\description{
Writes the snapshot's tracked values back into the registered
\code{reactiveValues}, runs the restore hooks, and sends the input values to the
browser, where the client script applies each one as soon as its input is
on the page. Inputs inside dynamic UI that appears (or re-renders) during
the restore receive their values without any timing configuration; while
the restore is in flight, shiny's \code{restoreInput()} mechanism is primed so
that dynamic UI is built with the restored values directly.
}
\details{
The function never blocks. It returns a handle containing a promise that
resolves to a \emph{restore report} once the browser reports that the page has
settled (no activity for \code{settle} seconds) or \code{timeout} seconds have passed.
The report is a data frame with one row per input and the columns \code{id},
\code{status}, \code{binding}, and \code{detail}, plus the attributes \code{txn}, \code{elapsed} (seconds),
\code{settled}, and \code{timed_out}. Statuses:
\itemize{
\item \code{applied}: sent to an input that was on the page.
\item \code{constructed}: the input appeared during the restore already carrying
the value, thanks to \code{restoreInput()}.
\item \code{reapplied}: the input re-rendered during the restore and received the
value again.
\item \code{missing}: the input never appeared before the restore settled.
\item \code{failed}: the input's binding raised an error.
\item \code{mismatched}: applied, but the input reports a different value
afterwards (for example a select whose choices do not contain it).
\item \code{skipped}: excluded, or its restorer chose not to restore it.
}

Preparation errors (an unreadable file, a failing \code{validate} or \code{migrate}
hook, a file from a different app) are raised immediately. Problems during
the transaction reject the promise: a second \code{snap_restore()} cancels the
first (condition class \code{shinysnap_cancelled}), a failing hook, and, with
\code{unknown = "error"}, inputs that never appeared or failed.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    observeEvent(input$restore_from_text, {
      snap_restore(input$json, on_done = function(report) print(report))
    })
  }
}
}
