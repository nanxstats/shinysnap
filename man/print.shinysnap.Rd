% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore.R, R/snapshot.R
\name{print.shinysnap_restore}
\alias{print.shinysnap_restore}
\alias{print.shinysnap_report}
\alias{print.shinysnap}
\alias{format.shinysnap}
\alias{as.list.shinysnap}
\alias{print.shinysnap_diff}
\title{Print, format, and coerce snapshots and restore results}
\usage{
\method{print}{shinysnap_restore}(x, ...)

\method{print}{shinysnap_report}(x, ...)

\method{print}{shinysnap}(x, ...)

\method{format}{shinysnap}(x, ...)

\method{as.list}{shinysnap}(x, ...)

\method{print}{shinysnap_diff}(x, ...)
}
\arguments{
\item{x}{A snapshot object, a snapshot comparison from \code{\link[=snap_diff]{snap_diff()}}, or
a restore handle or report from \code{\link[=snap_restore]{snap_restore()}}. \code{format()} and
\code{as.list()} accept snapshot objects only.}

\item{...}{Passed to \code{\link[=print.data.frame]{print.data.frame()}} when printing a snapshot
comparison or restore report; ignored otherwise.}
}
\value{
\code{print()} returns \code{x} invisibly; \code{format()} a character vector;
\code{as.list()} a plain list.
}
\description{
\code{print()} shows a compact summary: app name and version, creation time,
and the number and names of inputs, values, and attachments. \code{format()}
returns the same lines as a character vector. \code{as.list()} drops the class
and returns the underlying list.
}
\details{
For a \code{\link[=snap_diff]{snap_diff()}} result, \code{print()} shows the changed entries and their
old and new values. For a \code{\link[=snap_restore]{snap_restore()}} handle, it shows the transaction
id; for a completed restore report, it shows the input statuses and the
elapsed time.
}
\examples{
snap <- snap_unserialize('{
  "format": 1,
  "app": {"name": "myapp", "version": "2.4.1"},
  "created": "2026-09-16T18:22:03Z",
  "inputs": {"n": 100, "rate": 0.025}
}')
print(snap)
format(snap)
names(as.list(snap))
print(snap_diff(snap, list(inputs = list(n = 50, rate = 0.025))))

if (interactive()) {
  library(shiny)
  ui <- fluidPage(
    numericInput("n", "n", 0),
    actionButton("restore", "Restore state")
  )
  server <- function(input, output, session) {
    observeEvent(input$restore, {
      handle <- snap_restore(list(inputs = list(n = 100)), on_done = print)
      print(handle)
    })
  }
  shinyApp(ui, server)
}
}
