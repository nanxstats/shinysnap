% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/restore.R
\name{snap_is_restoring}
\alias{snap_is_restoring}
\title{Is a restore in flight?}
\usage{
snap_is_restoring(session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{session}{The Shiny session. Defaults to the current session.}
}
\value{
A logical scalar.
}
\description{
\code{TRUE} from the moment \code{\link[=snap_restore]{snap_restore()}} is called until the browser reports
that the restore has settled (or timed out), \code{FALSE} otherwise. Inside a
reactive context the result is reactive, so observers and outputs can
react to the end of a restore; outside one it is a plain value.
}
\details{
Use it to keep expensive observers from running on every intermediate
value during a restore, and to run something once the state is complete.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    observeEvent(input$n, {
      if (snap_is_restoring()) {
        return()
      }
      message("user changed n to ", input$n)
    })
  }
}
}
