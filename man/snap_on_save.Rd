% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hooks.R
\name{snap_on_save}
\alias{snap_on_save}
\title{Register a hook that runs when a snapshot is taken}
\usage{
snap_on_save(fn, session = shiny::getDefaultReactiveDomain())
}
\arguments{
\item{fn}{A function taking one argument, \code{state}.}

\item{session}{The Shiny session. Defaults to the current session.}
}
\value{
A function that removes the hook again, invisibly.
}
\description{
\code{snap_on_save()} registers a function that \code{\link[=snap_take]{snap_take()}} calls after the
inputs and tracked values have been collected. It receives a \code{state}
object: \code{state$inputs} is the named list of captured input values and
\code{state$values} is an environment. Anything the hook assigns into
\code{state$values} is stored in the snapshot's \code{values} section, so several
hooks (and several modules) can contribute without overwriting each other,
just like shiny's \code{onBookmark()}.
}
\details{
Inside a module, the hook sees only the module's inputs (with the
namespace prefix removed) and its values are stored under namespaced
names automatically.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    snap_on_save(function(state) {
      state$values$fitted_at <- format(Sys.time())
    })
  }
}
}
