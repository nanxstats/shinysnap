% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/enable.R
\name{snap_enable}
\alias{snap_enable}
\title{Configure shinysnap for a session}
\usage{
snap_enable(
  app = NULL,
  version = NULL,
  exclude = NULL,
  include = NULL,
  live_only = TRUE,
  use_restore_context = TRUE,
  verbose = FALSE,
  session = shiny::getDefaultReactiveDomain()
)
}
\arguments{
\item{app, version}{The name and version of the app, written into every
snapshot file. They default to the options \code{shinysnap.app} and
\code{shinysnap.version}, which packaged apps can set once in \code{.onLoad()}.}

\item{exclude, include}{Character vectors of regular expressions matched
against fully namespaced input ids. Matching \code{exclude} patterns are
never captured; when \code{include} is given, only matching ids are. Ids
excluded with shiny's \code{setBookmarkExclude()} are always honoured too.}

\item{live_only}{Capture only inputs that are currently on the page (as
reported by the client script), dropping values of inputs whose UI has
been removed. Set to \code{FALSE} to capture every value shiny remembers.}

\item{use_restore_context}{Prime shiny's \code{restoreInput()} mechanism during
a restore, so that dynamic UI re-rendered during the restore is built
with the restored values. Turn off only to debug.}

\item{verbose}{Print messages about what is captured, dropped, and
restored.}

\item{session}{The Shiny session. Defaults to the current session.}
}
\value{
The per-session controller, invisibly.
}
\description{
Sets the app identity and the defaults used by \code{\link[=snap_take]{snap_take()}} and
\code{snap_restore()} for the current session. Calling it is optional: every
\verb{snap_*()} server function creates the per-session state on first use with
the defaults below. Call it again to change the settings.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    snap_enable(app = "myapp", version = "2.4.0", exclude = c("^btn_", "^nav_"))
  }
}
}
