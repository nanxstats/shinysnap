% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/snapshot.R
\name{snap_take}
\alias{snap_take}
\title{Take a snapshot of the running app}
\usage{
snap_take(
  session = shiny::getDefaultReactiveDomain(),
  ...,
  include = NULL,
  exclude = NULL,
  live_only = NULL,
  values = TRUE,
  scope = c("root", "module"),
  meta = list()
)
}
\arguments{
\item{session}{The Shiny session. Defaults to the current session.}

\item{...}{Not used; arguments after \code{session} must be named.}

\item{include, exclude}{Regular expressions matched against fully
namespaced ids, in addition to those configured with \code{\link[=snap_enable]{snap_enable()}}.}

\item{live_only}{Keep only inputs currently on the page. Defaults to the
value configured with \code{\link[=snap_enable]{snap_enable()}} (\code{TRUE}).}

\item{values}{Capture tracked values and run the save hooks? \code{FALSE}
captures inputs only.}

\item{scope}{\code{"root"} (the default) captures the whole app with full ids,
even when called inside a module; \code{"module"} keeps only ids under the
calling module's namespace (still as full ids).}

\item{meta}{A named list of free-form metadata stored in the snapshot.}
}
\value{
A snapshot object of class \code{shinysnap}.
}
\description{
Captures the current input values and the registered server-side values
of the session into a snapshot object, ready for \code{\link[=snap_write]{snap_write()}} or
\code{\link[=snap_serialize]{snap_serialize()}}. Nothing is written to disk.
}
\details{
What is captured:
\itemize{
\item Inputs, by fully namespaced id, exactly as \code{input$id} returns them.
With \code{live_only}, only inputs that are currently on the page are
kept, so values of inputs whose dynamic UI has been removed are not
carried along. Action buttons, password inputs, and values that shiny's
own serializers mark as unserializable are never captured; ids excluded
with \code{setBookmarkExclude()}, \code{\link[=snap_exclude]{snap_exclude()}}, or the \code{exclude} patterns
are dropped. File inputs are moved to the snapshot's \code{attachments}.
\item The name of the client-side input binding of each captured input, in
\code{bindings}.
\item Values: the fields of every \code{reactiveValues} registered with
\code{\link[=snap_track]{snap_track()}}, then whatever the \code{\link[=snap_on_save]{snap_on_save()}} hooks add.
}

The function isolates every read, so it never creates reactive
dependencies. Inputs with a rate policy (text inputs debounce, sliders
throttle) may lag the browser by a few hundred milliseconds; a snapshot
taken from a download handler runs after the click has reached the
server, which in practice is later than that.
}
\examples{
if (interactive()) {
  library(shiny)

  server <- function(input, output, session) {
    observeEvent(input$show, {
      print(snap_take())
    })
  }
}
}
